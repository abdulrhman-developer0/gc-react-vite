/**
 * Define your helpers functions here to use in project as globly.
 * gides:
 *      1 functions take inputs and return output  for your client code.
 *      2 functions most be small and usable
 */

/**
 * say hello to user.
 *
 * @param name
 * @return `hello ${name}`
 */
const sayHello = (name) => {
  alert(`hello ${name}`);
};
